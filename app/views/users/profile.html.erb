<p> <%= @user.avatar %></p>
<h1><%= @user.username %></h1>
<p>Joined: <%= @user.created_at %></p>
<p>Last active: <%= @user.last_active_at %> </p>
<p>Points: <%= @user.points %></p>
<p>Badges:</p>
<% @user.badges.each.with_index(1) do |badge, i| %>
	<% if i%4 == 1 %>
		<div class="row">
	<% end %>
	<div class="col-sm-3">
		<%= image_tag("badges/#{badge.image}.jpeg", size: "50x50") %>
		<p><%= badge.name %></p>
	</div>
	<% if i%4 == 0 || badge.id == @user.badges.last.id %>
		</div>
	<% end %>
<% end %>

<p>Friends (<%= "#{@user.friends.count}" %>)</p>
<% if !@user.friendships.empty? %>
  <% @user.friendships.each.with_index(1) do |f, i| %>
    <% if i%4 == 1 %>
      <div class="row">
    <% end %>
    <div class="col-sm-3">
      <p><%= link_to f.friend.username, user_path(f.friend) %></p>
      <p><%= link_to 'Unfriend', friend_destroy_path(friend: f.friend) %></p>
    </div>
    <% if i%4 == 0 || f.friend.id == @user.friendships.last.friend.id %>
      </div>
    <% end %>
  <%end%>
<%else%>
<p>No friends to show</p>
<% end %>